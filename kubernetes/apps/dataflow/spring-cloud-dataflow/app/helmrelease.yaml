---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/helm.toolkit.fluxcd.io/helmrelease_v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: spring-cloud-dataflow
  namespace: dataflow
spec:
  interval: 5m
  chart:
    spec:
      chart: spring-cloud-dataflow
      version: 26.11.0
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system

  maxHistory: 2

  install:
    createNamespace: true
    timeout: 1m
    remediation:
      retries: 3

  upgrade:
    cleanupOnFail: true
    timeout: 1m
    remediation:
      retries: 3

  uninstall:
    keepHistory: false

  values:

    server:

      configuration:
        metricsDashboard: https://grafana.dmfrey.com:443

      resources:
        requests:
          cpu: "0.5"
          memory: 1024M
        limits:
          cpu: "1"
          memory: 2048M

      service:
        type: LoadBalancer
        port: 8080

      ingress:
        enabled: true

        annotations:
          nginx.org/websocket-services: dataflow
          cert-manager.io/cluster-issuer: letsencrypt-prod
          hajimari.io/enable: "true"
          # hajimari.io/icon: "simple-icons:"
          external-dns.alpha.kubernetes.io/target: external.${SECRET_DOMAIN}
        ingressClassName: external
        hostname: &host dataflow.${SECRET_DOMAIN}
        tls: true

    metrics:
      enabled: true

      resources:
        limits:
          cpu: "1.0"
          memory: 2048Mi
        requests:
          cpu: "0.5"
          memory: 1024Mi
      
      replicaCount: 1
      
      serviceMonitor:
        enabled: true

    mariadb:
      enabled: false

    externalDatabase:
      driver: org.postgresql.Driver
      dataflow:
        url: jdbc:postgresql://pg-dataflow-rw.dataflow.svc.cluster.local:5432/dataflow
        username: dataflow
        existingSecret: pg-dataflow-app
        existingSecretPasswordKey: password
      skipper:
        url: jdbc:postgresql://pg-skipper-rw.dataflow.svc.cluster.local:5432/skipper
        username: skipper
        existingSecret: pg-skipper-app
        existingSecretPasswordKey: password

    rabbitmq:
      enabled: false
      auth:
        password: mbiX_yOfJUVqGIzpi2OViPzy2RzAgjmC
        erlangCookie: BJTCxSa65HU3CT_yyJGp2NntPMgoKqDE
      
    externalRabbitmq:
      enabled: true
      host: rabbit-dataflow.dataflow.svc.cluster.local
      port: 5672
      username: default_user_it2MwhamLIHA38GozD_
      password: mbiX_yOfJUVqGIzpi2OViPzy2RzAgjmC
