---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: spring-cloud-dataflow
  namespace: dataflow
spec:
  interval: 30m
  chart:
    spec:
      chart: spring-cloud-dataflow
      version: 25.1.3
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
      interval: 30m
  values:

    server:

      configuration:
        metricsDashboard: grafana.monitor.svc.cluster.local:80

      extraEnvVars: &extraEnvVars
        - name: dataflow.jdbcUrl
          valueFrom:
            secretKeyRef:
              name: pg-dataflow-app
              key: jdbc-url
        - name: skipper.jdbcUrl
          valueFrom:
            secretKeyRef:
              name: pg-skipper-app
              key: jdbc-url
        - name: rabbitmq.host
          valueFrom:
            secretKeyRef:
              name: rabbit-dataflow-default-user
              key: host
        - name: rabbitmq.port
          valueFrom:
            secretKeyRef:
              name: rabbit-dataflow-default-user
              key: port
        - name: rabbitmq.username
          valueFrom:
            secretKeyRef:
              name: rabbit-dataflow-default-user
              key: username
        - name: rabbitmq.password
          valueFrom:
            secretKeyRef:
              name: rabbit-dataflow-default-user
              key: password

      resources:
        limits:
          cpu: "1"
          memory: 1024M
        requests:
          cpu: "1"
          memory: 1024M

      service:
        type: LoadBalancer
        port: 8080

      ingress:
        enabled: true

        annotations:
          nginx.org/websocket-services: dataflow
          cert-manager.io/cluster-issuer: letsencrypt-prod
          hajimari.io/enable: "true"
          # hajimari.io/icon: "simple-icons:"
        ingressClassName: external-nginx
        hostname: &host dataflow.${EXTERNAL_DOMAIN}
        tls: true

    skipper:

      extraEnvVars: *extraEnvVars

    mariadb:
      enabled: false

    externalDatabase:
      dataflow:
        url: jdbc:postgresql://pg-dataflow-rw:5432/dataflow?password=QvjhDUBO00S8sXX9Ob3Rzg8EOrcXYZDChWh4YXtian87uJaAOUR3pcSbKWBSayyw&user=dataflow   # ${dataflow.jdbcUrl}
      skipper:
        url: jdbc:postgresql://pg-skipper-rw:5432/skipper?password=tn22t7FidJdwk22rcaVsf5giPas8DIxOa9mwWvCJCnLxXbObUnZviv1mspsd3tIw&user=skipper      # ${skipper.jdbcUrl}

    rabbitmq:
      enabled: false

    externalRabbitmq:
      enabled: true

      host: rabbit-dataflow.dataflow.svc          # ${rabbitmq.host}
      port: 5672                                  # ${rabbitmq.port}
      username: default_user_W7v4_AF-3WpglBOuQWN  # ${rabbitmq.username}
      password: lUItvt1Ipatf_7lCgV77Nn7AZ-cVjHup  # ${rabbitmq.password}
