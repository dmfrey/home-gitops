---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps-dataflow
  namespace: flux-system
spec:
  interval: 10m
  path: "./kubernetes/apps/dataflow/dataflow/app"
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-gitops
  wait: true
  decryption:
    provider: sops
  postBuild:
    substituteFrom: []
  dependsOn:
    - name: cluster-apps-dataflow-services
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps-dataflow-services
  namespace: flux-system
spec:
  interval: 10m
  path: "./kubernetes/apps/dataflow/dataflow/services"
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-gitops
  wait: true
  decryption:
    provider: sops
  postBuild:
    substituteFrom: []
