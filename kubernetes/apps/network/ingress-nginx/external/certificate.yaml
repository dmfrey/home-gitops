---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "${ORGANIZATION}-external"
spec:
  secretName: "${ORGANIZATION}-tls-external"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.${EXTERNAL_DOMAIN}"
