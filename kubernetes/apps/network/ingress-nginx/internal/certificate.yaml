---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "${ORGANIZATION}-internal"
spec:
  secretName: "${ORGANIZATION}-tls-internal"
  issuerRef:
    name: homelab-k8s-001-internal-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.${INTERNAL_DOMAIN}"
