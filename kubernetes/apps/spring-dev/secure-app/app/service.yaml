apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: secure-app-bundle
  name: secure-app-bundle
spec:
  ports:
    - name: https
      port: 8443
      targetPort: 8443
  selector:
    app.kubernetes.io/name: secure-app-bundle
  type: ClusterIP
