---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/barmancloud.cnpg.io/objectstore_v1.json
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: home-assistant
  annotations:
    secret.reloader.stakater.com/reload: home-assistant-cnpg-s3-secret

spec:
  configuration:

    destinationPath: s3://cnpg/
    endpointURL: http://garage.garage-system.svc.cluster.local:3900

    s3Credentials:
      accessKeyId:
        name: home-assistant-cnpg-s3-secret
        key: AWS_SECRET_ACCESS_KEY_ID
      secretAccessKey:
        name: home-assistant-cnpg-s3-secret
        key: AWS_SECRET_ACCESS_KEY

    wal:
      compression: gzip
      maxParallel: 8

  retentionPolicy: 7d
