---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tempo-s3
spec:
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  refreshInterval: 15m
  target:
    name: tempo-s3-secret
    template:
      data:
        S3_ACCESS_KEY: "{{ .GARAGE_TEMPO_ACCESS_KEY }}"
        S3_SECRET_KEY: "{{ .GARAGE_TEMPO_SECRET_KEY }}"
  dataFrom:
    - extract:
        key: garage
