---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: romm-dragonflydb-s3
spec:
  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore
  refreshInterval: 15m
  target:
    name: romm-dragonflydb-s3-secret
    template:
      engineVersion: v2
      data:
        AWS_ACCESS_KEY_ID: "{{ .GARAGE_DRAGONFLYDB_ACCESS_KEY }}"
        AWS_SECRET_ACCESS_KEY: "{{ .GARAGE_DRAGONFLYDB_SECRET_KEY }}"
  dataFrom:
  - extract:
      key: garage
