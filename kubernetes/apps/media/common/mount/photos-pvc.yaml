---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos-nfs-media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: photos
  resources:
    requests:
      storage: 1Mi
