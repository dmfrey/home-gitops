---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos-nfs-media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: media
  resources:
    requests:
      storage: 1Mi
  volumeName: "photos-nfs-media"
