---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sonarqube
spec:
  chartRef:
    kind: OCIRepository
    name: sonarqube
  interval: 1h
  timeout: 30m
  values:
    controllers:
      sonarqube:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/library/sonarqube
              tag: 25.12.0.117093-community
              pullPolicy: IfNotPresent
            env:
              SONAR_JDBC_URL: jdbc:postgresql://sonarqube-cnpg-rw.security.svc.cluster.local:5432/sonarqube
              SONAR_JDBC_USERNAME:
                valueFrom:
                  secretKeyRef:
                    name: sonarqube-cnpg-app
                    key: username
              SONAR_JDBC_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: sonarqube-cnpg-app
                    key: password
              SONAR_WEB_CONTEXT: /
              SONAR_TELEMETRY_ENABLE: "false"
              # SAML / Authentik SSO
              SONAR_AUTH_SAML_ENABLED: "true"
              SONAR_AUTH_SAML_APPLICATIONID: sonarqube
              SONAR_AUTH_SAML_PROVIDERNAME: Authentik
              SONAR_AUTH_SAML_PROVIDERID: https://auth.dmfrey.com
              SONAR_AUTH_SAML_LOGINURL: https://auth.dmfrey.com/application/saml/sonarqube/sso/binding/redirect/
              SONAR_AUTH_SAML_CERTIFICATE_SECURED:
                valueFrom:
                  secretKeyRef:
                    name: sonarqube-secret
                    key: SONAR_AUTH_SAML_CERTIFICATE
              SONAR_AUTH_SAML_USER_LOGIN: http://schemas.goauthentik.io/2021/02/saml/username
              SONAR_AUTH_SAML_USER_NAME: http://schemas.microsoft.com/ws/2008/06/identity/claims/windowsaccountname
              SONAR_AUTH_SAML_USER_EMAIL: http://schemas.xmlsoap.org/ws/2005/05/identity/claims/emailaddress
              SONAR_AUTH_SAML_GROUP_NAME: http://schemas.xmlsoap.org/claims/Group
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/system/status
                    port: 9000
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 5
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/system/status
                    port: 9000
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 5
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/system/status
                    port: 9000
                  initialDelaySeconds: 60
                  periodSeconds: 15
                  timeoutSeconds: 5
                  failureThreshold: 120
            resources:
              requests:
                cpu: 500m
                memory: 2Gi
              limits:
                memory: 4Gi
    defaultPodOptions:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
    service:
      app:
        controller: sonarqube
        ports:
          http:
            port: &port 9000
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/icon: sonarqube.png
          gethomepage.dev/name: SonarQube
          gethomepage.dev/group: Security
          gethomepage.dev/app: sonarqube
          gethomepage.dev/description: Code Quality & Security
        hostnames:
          - sonarqube.dmfrey.com
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - name: sonarqube
                port: *port
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        advancedMounts:
          sonarqube:
            app:
              - path: /opt/sonarqube/data
      logs:
        type: emptyDir
        advancedMounts:
          sonarqube:
            app:
              - path: /opt/sonarqube/logs
      extensions:
        type: emptyDir
        advancedMounts:
          sonarqube:
            app:
              - path: /opt/sonarqube/extensions
      temp:
        type: emptyDir
        advancedMounts:
          sonarqube:
            app:
              - path: /opt/sonarqube/temp
