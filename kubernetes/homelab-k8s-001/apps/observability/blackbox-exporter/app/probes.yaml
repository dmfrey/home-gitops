# yaml-language-server: $schema=https://kubernetes-schemas-5e0.pages.dev/monitoring.coreos.com/probe_v1.json
---
kind: Probe
apiVersion: monitoring.coreos.com/v1
metadata:
  name: devices

spec:

  module: icmp

  prober:
    url: blackbox-exporter.observabiltiy.svc.cluster.local:9115

  targets:
    staticConfig:
      static:
        - minio.${SECRET_DOMAIN}
  #       - expanse.internal
  #       - garage-door.internal
  #       - hdhomerun.internal
  #       - idrac.internal
  #       - kiosk.internal
  #       - kvm.internal
  #       - pikvm.internal
  #       - receiver.internal
  #       - ups.internal
  #       - vacuum.internal
  #       - zigbee-controller.internal
  #       - zwave-controller.internal
