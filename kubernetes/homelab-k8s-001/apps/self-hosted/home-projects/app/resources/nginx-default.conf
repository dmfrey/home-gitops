server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Disable trailing-slash redirect for .html files
    location ~ \.html$ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
