---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authentik-kv-cluster
spec:
  selector:
    matchLabels:
      app: authentik-kv-cluster
  podTargetLabels: ["app"]
  podMetricsEndpoints:
    - port: admin
