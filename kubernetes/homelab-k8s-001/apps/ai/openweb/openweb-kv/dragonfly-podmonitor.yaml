---
# yaml-language-server: $schema=https://kubernetes-schemas.dmfrey.com/monitoring.coreos.com/podmonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: openweb-dragonflydb
spec:
  selector:
    matchLabels:
      app: openweb-dragonflydb
  podTargetLabels:
    - app
  podMetricsEndpoints:
    - port: admin
