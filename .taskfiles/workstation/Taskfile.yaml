---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

vars:
  WORKSTATION_RESOURCES_DIR: '{{.ROOT_DIR}}/.taskfiles/workstation/resources'

tasks:

  apt:
    desc: Set up APT tools
    deps: [setup-build]
    cmds:
      - sudo apt install -y ${cat {{.WORKSTATION_RESOURCES_DIR}}/apt_packages.txt}
    sources:
      - '{{.WORKSTATION_RESOURCES_DIR}}/apt_packages.txt'
    preconditions:
      - which apt
      - test -f {{.WORKSTATION_RESOURCES_DIR}}/apt_packages.txt

  brew:
    desc: Set up Homebrew tools
    cmds:
      - brew bundle --file {{.WORKSTATION_RESOURCES_DIR}}/Brewfile
    sources:
      - '{{.WORKSTATION_RESOURCES_DIR}}/Brewfile'
    generates:
      - '{{.WORKSTATION_RESOURCES_DIR}}/Brewfile.lock.json'
    preconditions:
      - which brew
      - test -f {{.WORKSTATION_RESOURCES_DIR}}/Brewfile

  krew:
    desc: Set up Krew tools
    deps: [brew]
    cmds:
      - kubectl krew install cert-manager cnpg browse-pvc node-shell rook-ceph view-secret
    preconditions:
      - kubectl krew version
      - which kubectl

  apt-update:
    internal: true
    cmds:
      - sudo apt update
    preconditions:
      - which apt

  setup-build:
    internal: true
    deps: [apt-update]
    cmds:
      - sudo apt install build-essential gpg wget curl
      - sudo install -dm 755 /etc/apt/keyrings

  add-deb-mise:
    internal: true
    cmds:
      - wget -qO - https://mise.jdx.dev/gpg-key.pub | gpg --dearmor | sudo tee /etc/apt/keyrings/mise-archive-keyring.gpg 1> /dev/null
      - echo "deb [signed-by=/etc/apt/keyrings/mise-archive-keyring.gpg arch=amd64] https://mise.jdx.dev/deb stable main" | sudo tee /etc/apt/sources.list.d/mise.list
      - task: apt-update
