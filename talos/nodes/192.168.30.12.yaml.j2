---
machine:
  type: controlplane
  nodeLabels:
    topology.kubernetes.io/zone: dmf-u
  install:
    diskSelector:
      model: Samsung SSD 870
    # 2026-07-25: RTX 3090 physically arrived and is plugged into the eGPU dock, but
    # still running the baseline (no nvidia extensions) schematic, not the eGPU one yet -
    # deliberately testing Thunderbolt link stability in isolation first (see
    # schematic.yaml.j2's thunderbolt.host_reset=0 re-add) before also introducing the
    # NVIDIA driver extensions as a second variable. Switch to `just talos
    # gen-schematic-id-egpu` once the dock holds a stable connection.
    # thunderbolt.host_reset=0 alone did not fix the hotplug command 0x1038 timeout
    # (confirmed on both hotplug-after-boot and dock-before-node sequencing); now also
    # testing pci=realloc,assign-busses, pcie_aspm=off, and pcie_port_pm=off stacked on
    # top of it. pcie_ports=native remains excluded.
    image: factory.talos.dev/installer/4d6d4728caf2ac4db0d9ca85acb8ece573a9f342e05283c96f97448a47552fdb:v1.13.7 # baseline + thunderbolt.host_reset=0 + pci=realloc,assign-busses + pcie_aspm=off + pcie_port_pm=off test - see `just talos gen-schematic-id`
    wipe: false
---
apiVersion: v1alpha1
kind: HostnameConfig
hostname: k8s-2
